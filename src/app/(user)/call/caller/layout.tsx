"use client";

import type React from "react";

/**
 * Layout for caller page - full screen call interface
 */
export default function CallerLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}


